<build>
  <plugins>
    <plugin>
      <groupId>com.phoenixnap.oss</groupId>
      <artifactId>springmvc-raml-plugin</artifactId>
      <version>2.0.5</version>
      <executions>
        <execution>
          <id>generate-server-stubs</id>
          <phase>generate-sources</phase>
          <goals>
            <goal>generate</goal>
          </goals>
          <configuration>
            <!-- Location of your RAML file -->
            <ramlPath>${project.basedir}/src/main/resources/api.raml</ramlPath>
            <!-- Output directory for generated code -->
            <outputPath>${project.build.directory}/generated-sources</outputPath>
            <!-- Additional configuration options, if required -->
          </configuration>
        </execution>
      </executions>
    </plugin>
  </plugins>
</build>
