<build>
  <plugins>
    <plugin>
      <groupId>org.raml</groupId>
      <artifactId>raml-jaxrs-codegen-maven-plugin</artifactId>
      <version>2.2.2</version>
      <configuration>
        <sourceDirectory>${project.basedir}/src/main/raml</sourceDirectory>
        <outputDirectory>${project.build.directory}/generated-sources/raml</outputDirectory>
      </configuration>
      <executions>
        <execution>
          <goals>
            <goal>generate</goal>
          </goals>
        </execution>
      </executions>
    </plugin>
  </plugins>
</build>
